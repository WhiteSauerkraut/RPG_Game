using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class devide : MonoBehaviour
{
    int a = 1, b = 2;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log((float)a / b);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
